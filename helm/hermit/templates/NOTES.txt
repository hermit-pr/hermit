H.E.R.M.I.T has been deployed as {{ include "hermit.fullname" . }}.

{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
Point your GitLab/GitHub webhook at the bot:

  GitHub: POST https://{{ .host }}/webhook/github  (validate with X-Hub-Signature-256)
  GitLab: POST https://{{ .host }}/webhook/gitlab  (validate with X-Gitlab-Token)
{{- end }}
{{- else }}
Point your GitLab/GitHub webhook at the bot so it can publish a review on
every PR/MR:

  GitHub: POST https://<host>/webhook/github  (validate with X-Hub-Signature-256)
  GitLab: POST https://<host>/webhook/gitlab  (validate with X-Gitlab-Token)
{{- end }}

Reviews are also triggered when someone writes "@hermit" in a PR/MR comment.

The chart stores RW and read-only tokens in the ``{{ include "hermit.fullname" . }}-tokens``
Secret. Other secrets (webhookSecret, vllmApiKey, jobIdSigningKey) reference
existing Kubernetes Secrets by name and key (same namespace). Verify those
references resolve and hold:

- webhookSecret: matches the secret configured on your webhook,
- githubRwToken / gitlabRwToken: lets the bot post reviews,
- gitReadToken: lets reviewer pods clone the repository,
- vllmApiKey (optional): forwarded to your vLLM endpoint by reviewer pods.

H.E.R.M.I.T only ever posts review comments; it never merges or commits.

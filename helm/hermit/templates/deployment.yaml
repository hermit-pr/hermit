apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hermit.fullname" . }}
  labels:
    {{- include "hermit.labels" . | nindent 4 }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "hermit.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "hermit.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      serviceAccountName: {{ .Values.config.serviceAccountName }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          envFrom:
            - configMapRef:
                name: {{ include "hermit.fullname" . }}
          env:
            {{- if .Values.githubRwToken.token.secretName }}
            - name: HERMIT_GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.githubRwToken.token.secretName | quote }}
                  key: {{ .Values.githubRwToken.token.secretKey | quote }}
            {{- end }}
            {{- if .Values.gitlabRwToken.token.secretName }}
            - name: HERMIT_GITLAB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.gitlabRwToken.token.secretName | quote }}
                  key: {{ .Values.gitlabRwToken.token.secretKey | quote }}
            {{- end }}
            {{- if .Values.githubReadToken.token.secretName }}
            - name: HERMIT_GIT_READ_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.githubReadToken.token.secretName | quote }}
                  key: {{ .Values.githubReadToken.token.secretKey | quote }}
            {{- end }}
            {{- range $i, $org := .Values.githubRwToken.orgs }}
            - name: HERMIT_GITHUB_RW_ORG_{{ $i }}_NAME
              value: {{ $org.name | quote }}
            - name: HERMIT_GITHUB_RW_ORG_{{ $i }}_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ $org.secretName | quote }}
                  key: {{ $org.secretKey | quote }}
            {{- end }}
            {{- range $i, $org := .Values.githubReadToken.orgs }}
            - name: HERMIT_GIT_READ_ORG_{{ $i }}_NAME
              value: {{ $org.name | quote }}
            - name: HERMIT_GIT_READ_ORG_{{ $i }}_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ $org.secretName | quote }}
                  key: {{ $org.secretKey | quote }}
            {{- end }}
            {{- if .Values.secrets.webhookSecret.name }}
            - name: HERMIT_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.webhookSecret.name | quote }}
                  key: {{ .Values.secrets.webhookSecret.key | required "secrets.webhookSecret.key is required" | quote }}
            {{- end }}
            {{- if .Values.secrets.vllmApiKey.name }}
            - name: HERMIT_VLLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.vllmApiKey.name | quote }}
                  key: {{ .Values.secrets.vllmApiKey.key | required "secrets.vllmApiKey.key is required" | quote }}
            {{- end }}
            {{- if .Values.secrets.jobIdSigningKey.name }}
            - name: HERMIT_JOB_ID_SIGNING_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.jobIdSigningKey.name | quote }}
                  key: {{ .Values.secrets.jobIdSigningKey.key | required "secrets.jobIdSigningKey.key is required" | quote }}
            {{- end }}
            {{- if or .Values.caBundle.configMap .Values.caBundle.secret }}
            {{- $key := .Values.caBundle.secretKey }}
            {{- if .Values.caBundle.configMap }}
            {{- $key = .Values.caBundle.configMapKey }}
            {{- end }}
            {{- $caPath := printf "%s/%s" .Values.caBundle.mountPath $key }}
            - name: SSL_CERT_FILE
              value: {{ $caPath | quote }}
            - name: REQUESTS_CA_BUNDLE
              value: {{ $caPath | quote }}
            - name: GIT_SSL_CAINFO
              value: {{ $caPath | quote }}
            - name: CURL_CA_BUNDLE
              value: {{ $caPath | quote }}
            - name: NODE_EXTRA_CA_CERTS
              value: {{ $caPath | quote }}
            {{- end }}
          volumeMounts:
            {{- if or .Values.caBundle.configMap .Values.caBundle.secret }}
            - name: ca-bundle
              mountPath: {{ .Values.caBundle.mountPath | quote }}
              readOnly: true
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 0
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        {{- if .Values.caBundle.configMap }}
        - name: ca-bundle
          configMap:
            name: {{ .Values.caBundle.configMap | quote }}
        {{- else if .Values.caBundle.secret }}
        - name: ca-bundle
          secret:
            secretName: {{ .Values.caBundle.secret | quote }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}